apiVersion: 1

datasources:
  - name: 'grafana-akips-integration'
    type: 'observiply-akips-datasource'
    access: proxy
    isDefault: false
    orgId: 1
    version: 1
    editable: true
    jsonData:
      url: '${AKIPS_URL:}'
      username: '${AKIPS_USERNAME:}'
      skipTLS: false
